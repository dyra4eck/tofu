default:
  image:
    name: ghcr.io/opentofu/opentofu:1.10.6
    entrypoint: [""]
  tags:
    - tf

validate:
  script:
    - tofu fmt -check -recursive -diff
    - tofu init -backend=false
    - tofu validate
    - tofu plan